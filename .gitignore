node_modules
dist
output
.playwright-cli
playwright-report
test-results
__pycache__
*.pyc
assets/source-originals
src/data/sources
*.log
*.tsbuildinfo
.env.local
.env.*.local
firebase-adminsdk-*.json
firebase-service-account*.json
android/app/google-services.json
*.jks
*.keystore
*.aab
.motman.sqlite*
.motman-social.json
.motman-matches.json
android/.idea/
supabase/.temp/
android/gradle/gradle-daemon-jvm.properties
.fuse_hidden*

# Brouillons de fabrication, jamais lus par l'application ni par le build.
#   tmp/pglast   — une dependance Python recopiee dans le depot, binaire
#                  Windows compris (.pyd), qu'aucun script n'importe.
#   tmp/imagegen — les etapes intermediaires des emblemes de rang ; les
#                  versions finies vivent dans `public/assets/ranks/`.
# 489 fichiers et 38 Mo qui alourdissaient chaque copie de travail. Ils restent
# sur le disque, et l'historique les garde : `git show <commit>:<chemin>` les
# rend si besoin.
tmp/

# Sauvegardes de la base (scripts/export_supabase_backup.ps1).
# Elles contiennent des DONNÉES PERSONNELLES : adresses e-mail, pseudos,
# signalements rédigés par les joueurs. Elles ne doivent jamais entrer dans le
# dépôt — un dépôt public ou un artefact GitHub les sortirait de France, en
# contradiction avec la politique de confidentialité de MotMan.
# Le script écrit par défaut HORS du dépôt ; ces lignes ne sont qu'un filet de
# sécurité si quelqu'un redirige l'export ici par erreur.
/roles.sql
/schema.sql
/data.sql
motman-sauvegardes/
sauvegardes/

# >>> atelier privé
# Tout ce qui fabrique les grilles, et donc en porte les SOLUTIONS : les
# scripts Python et leurs données, le catalogue source et sa projection, la
# boîte de dépôt de l'Éditeur (ses lots approuvés, `review.html` compris). Le
# dépôt est public : ces fichiers publieraient les réponses des défis à venir.
#
# Ils sont suivis par l'ATELIER, un dépôt git privé superposé à cette copie de
# travail (`npm run atelier`), jamais par ce dépôt-ci. `scripts/atelier.mjs`
# lit CE bloc pour savoir quoi suivre : c'est la seule liste. Et
# `npm run check:atelier` échoue si un fichier suivi ici tombe sous un motif.
/scripts/*.py
/scripts/tests/*.py
/src/data/grid.catalog.json
/src/data/runtime.grid.catalog.json
/src/data/grid-generation*/
/src/data/pool/
/src/data/.grid-generation.lock
/src/data/bench-hard-*.json
/src/data/candidate-*.json
/src/data/easy-candidate-*.json
/src/data/easy-v2-candidate-*.json
/src/data/csp-*.json
/src/data/csp2-*.json
/src/data/smoke-*.json
/src/data/generated.easy.json
/src/data/crossword.*
/src/data/jeuxdemots.*
/src/data/lexique.*
/src/data/fill.wordlist.large.*
/MotMan Grilles prêtes/
# <<< atelier privé

# Le dossier git de l'atelier lui-même.
/.atelier-git/
